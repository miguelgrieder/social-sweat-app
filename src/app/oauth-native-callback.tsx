import React from 'react';
import Loading from '@/components/Loading';

const OAuthNativeCallback = () => {
  console.log('OAuthNativeCallback');

  return <Loading />;
};

export default OAuthNativeCallback;
